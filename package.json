{
  "name": "template_node-ts",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf ./dist",
    "build": "tsup src/* --config 'tsup.config.dev.json'",
    "build-prod": "tsup --minify",
    "start-watch": "tsup src/* --config 'tsup.config.dev.json' --watch --onSuccess 'node dist/index.js'",
    "start": "tsup src/* --config 'tsup.config.dev.json' --onSuccess 'node dist/index.js'",
    "start-prod": "tsup --minify --onSuccess 'node dist/index.js'",
    "test": "vitest --run",
    "test-related": "vitest --run related",
    "test-watch": "vitest"
  },
  "type": "module",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^18.11.18",
    "happy-dom": "^8.1.4",
    "nodemon": "^2.0.20",
    "tsup": "^6.5.0",
    "typescript": "^4.9.4",
    "vitest": "^0.27.1"
  }
}
